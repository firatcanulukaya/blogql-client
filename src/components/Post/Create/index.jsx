import UserCard from "../../UserCard";
import CreatePost from "./CreatePost";

const Create = () => {
    return (
        <div>
            <UserCard/>
            <CreatePost/>
        </div>
    );
}

export default Create;